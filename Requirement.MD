Tech Requirements

1. Uses an LLM to discover UI workflows through observe-decide-act loops
2. Records successful runs as typed, versioned capability artifacts
3. Replays artifacts deterministically without LLM involvement
4. Handles runtime errors, business outcomes, and human escalation
5. Enforces safety guardrails and data redaction